/**
 * The effect plane: a vocabulary, no service.
 *
 * Everything lives in `events.ts` so that a module below any future service —
 * crash repair and the approval seam's consent subject today, the effect audit
 * later — can name an effect without importing a seam. There is no `Effects`
 * key and no provider interface: a record is a session fact, and the session
 * already knows how to carry one.
 */
export * from './events.ts'
