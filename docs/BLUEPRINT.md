# MiniDSH Blueprint

What comes next, the route there, what is undecided, and a compact record of how the system arrived. `ARCHITECTURE.md` (beside this file) is the current shape and its limitations; this file is direction and the measurements the architecture map does not carry. Its budget is in `scripts/check-docs.ts`; past 90%, compress §4 first (older entries collapse to what a later session reuses).

## 1. Next session — S16.5 Hardening checkpoint A = the 1.1 release candidate

The first open entry of the route in §2. **Purpose:** the record plane (S14, S16) and the authority plane (S15) reviewed as ONE system before the runtime widens, then released as `minidsh@1.1.0`.

**Step 0 (CLAUDE.md §6, a `.5`).** A repo-wide read; every §4 and §7 invariant re-audited against its owning code; the current DSH re-researched broadly and the pin moved from `ddefc45f`/`c36a83ff` (session-durability is already at `477b4f42`); every ledger row rerun, the ledger merged into the area files (deferred from S15.1); each *Not read* list worked through.

**Work.** (1) **Compaction**, delegated per the `docs-maintenance` skill: ARCHITECTURE sits at 90% of its ceiling and README at 93%. (2) **Release**: the version from `1.1.0-dev` to `1.1.0` (every `composition/applied` names it, so a writer is attributable), a CHANGELOG cut, the packed-tarball gates, and publishing from CI (OIDC trusted publishing from a `release.yml` on a `v*` tag with `--provenance`, removing the interactive-2FA step). (3) What S16 left open: a power cut's zero-filled END reads torn, but zeros FOLLOWED by a surviving line read damaged and need `--salvage` — whether a synced lifecycle's case should repair on its own; `verify` checks no payload beyond surface messages and its report's folds (ARCHITECTURE §13); whether `GOOGLE_APPLICATION_CREDENTIALS` should reach the shell child; hard links under Seatbelt, unmeasured; the web arc's replay losing a file the model wrote by absolute path (1 of 8 S16 runs). (4) The routing arc's TWO failure shapes reported separately: the marker drop with `history_read` called 0×, and the model going off-instruction with the information reachable, which is a prompt problem.

**Gate.** A release candidate: `pnpm check` on Windows and under WSL 2 with both `MINIDSH_EXPECT_*` flags, every arc three times per host, `live.yml` on the pushed candidate, and a review of the two planes as one.

## 2. Macro route (dependency-ordered; sessions are approximate)

| Session | Architectural outcome | Depends on |
|---|---|---|
| ~~S1–S13, V1~~ | ✅ The kernel and the core spine; two adapters and three tools; JSONL persistence with resume and fork; the client protocol on three carriers; authority as a durable plane with two confinement backends; composition layers; compaction and bounded recall; routing and delegation; the web surface; the attachment plane; `minidsh@1.0.0` on npm; this route and `references/`. Two hardening checkpoints and a docs compaction. **§4 is the record.** | — |
| ~~**S14** Durable execution I (2026-09-20)~~ | ✅ Every unfinished bracket closed after a crash; a durable gate-to-body fact; an effect record for the two effect families; the delegating call on the child. §4. | S13 |
| ~~**S15** Authority II (2026-09-23)~~ | ✅ A trusted consent subject and the whole call on the line; who decided and under which consent; shell-enforcement ACCEPTANCE as its own knob (the Windows fix); session-lifetime grants with revocation; the shell child's environment scrubbed. §4. | S14 |
| ~~**S15.1** Documentation reset (2026-09-24)~~ | ✅ The documents became maps sized to a reading load; references a navigation layer; compaction delegated. §4. | S15 |
| ~~**S16** Durable execution II (2026-09-26)~~ | ✅ The header as the one format mark; a per-lifecycle record of what its writer guaranteed; synced checkpoints, measured; cold `sessions verify` and `inspect`; an audit of effects; salvage; an effect-free replay over two real fixtures (kept in `test-support`: no shipped consumer). §4. | S14, S15 |
| **S16.5** Hardening checkpoint A = 1.1 release candidate | the record and authority planes reviewed as one system | S14–S16 |
| **S17** Jobs: owner-scoped background work | `core/jobs`; durable `job/*` brackets spanning turns; completion only through the inbox; a held process in the shell world; a control-plane tier on the wire | S16.5 |
| **S18** Providers and the first network tool *(movable)* | an OpenAI adapter on the Responses API; web search as an opt-in `NETWORK`-tagged row | S15 |
| **S19** Skills as data *(movable)* | one `skills` row; the catalog a durable user message; bodies through ordinary reads; four document skills | S15 |
| **S20** Browser verification | `tool-browser` over a CDP pipe to an installed Chrome; an agent-scoped process; screenshots as attachments; a verifier child; the dev server as a job | S15, S17 |
| **S20.5** Hardening checkpoint B = 1.2 release candidate | the widened runtime reviewed as one system | S17–S20 |
| **S21** More than one actor | continuable children and bounded parallel tool calls, only if S17–S20 evidence justifies them | S20.5 |
| **S22** Surfaces: product pass and the Desktop launcher | fork and resume on the wire; terminal and browser quality; uploads; tool display facts; Desktop = launcher + webview | S17 |
| **S22.5** Hardening checkpoint C = V2 release candidate | — | S21, S22 |

**The ordering principle: deepen the record before widening the runtime.** Every later capability adds a new kind of effect or a new concurrent actor, and each must be expressible as log facts, under recorded authority, with a recovery rule, so those three come first. One concept runs through the first cluster: a core **effect vocabulary** (what a call is about to do and what it did, written by trusted code from validated arguments, never model prose) with three consumers: the crash record (S14), the consent subject and grants (S15), the audit of effects (S16). Authority precedes jobs because on Windows a background job cannot exist while every shell call is a one-shot escalation. S18 and S19 need no new seam and may be pulled earlier on demand. **Checkpoints follow clusters of architectural change**, never every session: the record and authority planes; the widened runtime; multi-actor and product. Each `.5` does what CLAUDE.md §6 says. S16.5 also takes what S13 parked: publishing from CI (OIDC trusted publishing from a `release.yml` on a `v*` tag with `--provenance`, which also removes the interactive-2FA step), and the routing arc's TWO failure shapes reported separately: the marker drop with `history_read` called 0×, and the model going off-instruction with the information reachable, which is a prompt problem, not a compaction problem.

**S17 — jobs.** *Does:* a `core/jobs` Definition (a 19th key) shipping its driver. A job is owned by an agent scope (one `ctx.effect`), has a `job/start|end` bracket that may span turns with an exactly-once terminal fact, completes ONLY through the durable inbox (busy: next step; idle: a bounded waking insert whose budget only user input refills), is bounded by REFUSAL at start, and closes `lost` when its host died (S14's closers; no re-adoption). First producer: a handle-returning verb on the `shell` Definition built from the existing one-shot child (own process group, reaped at disposal), output to spill. Tools `job_start`, `job_output`, `job_cancel`; `SessionView.jobs` folded from the log; a cancel verb; the deployment-scope control-plane subscription (a summary push that ignores narrowing, answerer candidacy tied to it; never job state dressed as durable wire events, and a place in the backpressure shedding order); the headless rule for a live job at `whenIdle`. DSH's jobs are in-memory and leave no session events: MiniDSH diverges on purpose. *Keeps:* the inbox is the only queue; a producer may not start work its owner cannot collect; a job runs under the session stamp and S15's acceptance, never a one-shot escalation; output is never trace events. *Not:* a scheduler, retries, dependencies, mailboxes, a generic task supertype, parallel tool calls.

**S18 — providers and the first network tool (movable).** `capabilities/llm-openai` on the Responses API (encrypted reasoning and item ids as the `ReplayEnvelope`; unsupported options refused before I/O; honest duplication of the HTTP plumbing); the routing arc gains an OpenAI leg; DeepSeek's unknown-id facts made conservative and its model-id rename absorbed; `capabilities/tool-web-search` (Tavily) as an OPT-IN row (`CredentialRef`, `NETWORK` consent, structured sources only, spill, a fixed untrusted-content notice); `web_fetch` only behind a full SSRF guard, or not at all. *Not:* generic OpenAI-compatible routing (when a third chat-completions provider is wanted, with facts from row config), a fetched catalog, a shared wire package in core.

**S19 — skills as data (movable).** After S15 because a document skill runs scripts through the shell, which Windows can only use freely once acceptance exists. One `capabilities/skills` row; discovery roots as row config from `app/home.ts`; the catalog is ONE durable plugin-sourced `user/message` (the workspace-instructions pattern: a non-empty first batch only, re-entered after a compaction shadows it), listing name, description, absolute path and origin class; bodies load through the existing editor or shell read, so no skill tool and no log kind; front-matter beyond name and description is ignored; a workspace skill cannot shadow by name (DSH gives workspace skills top precedence with no trust gate: a warning). docx, pdf, xlsx and pptx as DATA: zero-dependency `.mjs` helpers plus host runtimes probed, degrading by stating what was not verified; a packaging assertion; a live arc asserting the WORLD. *Not:* a marketplace, skills registering tools or hooks, per-skill authority or model, a PDF attachment kind, a watcher.

**S20 — browser verification.** `capabilities/tool-browser`: a hand-written CDP client over `--remote-debugging-pipe` to an installed Chrome or Edge, a functional probe at mount, the process owned by the live agent scope and never restored from the log; text tools first; screenshots through a core image-admission helper moved out of `tool-view-image` (step 0); the verifier child via `model-roles`; the dev server as an S17 job; a private profile, downloads off, the environment scrubbed, no loopback debug port a confined shell could reach; `MINIDSH_EXPECT_BROWSER`. *Not:* a `core/browser` Definition before a second provider, Playwright, computer use, a tool running its own model without a durable `llm/aux-call`.

**S21 — more than one actor.** Step 0: replay keyed by `subagent/start{callId, childId}`, never first-request order. A continuable child is a durable child session, a resident activation and the child's own inbox (no jobs, as upstream); `send` and `interrupt` (cancel keeping the inbox); `ResumeAgentOptions.setup` so narrowing survives a resume; runtime-authored notices under a distinct source kind; the one-shot background child as the second job kind; parallel tool calls through a per-call, pure, fail-closed `isConcurrencySafe(args)` with results committed in model order. *Not:* agent teams, mailboxes, schedulers, swarm presets.

**S22 — surfaces.** `session/fork` and resume-without-prompt on the wire, the terminal's beside-the-wire bypass deleted; `publishView` on a status change; a graceful web stop and `web --json`; a terminal line editor and status line; browser markdown, cards, a model picker, uploads and attachment reads; tool KIND on the wire so no surface hard-codes a tool name; Desktop as a packaged launcher plus a webview over the existing carrier; the importable-surface decision; the remaining visual assets (a terminal recording, a browser screenshot, a social preview). *Not:* a second runtime, surface-held state.

**Still refused, with the reason:** model-written extensions (upstream replaced its own with gated plugin installs; `node:vm` is no boundary); an MCP bridge (no demonstrated need); a Windows confinement backend (native bindings, standing ACL changes, `partial` today) and Landlock (a prebuilt native launcher); session deletion (no authority above the append-only seam, none upstream); PTC; a session search index; per-user identity; agent teams.

## 3. Open questions (a decision is pending; the limitations themselves live in ARCHITECTURE §13)

- **What does a resumed session learn about a `lost` job, and how does replay order a completion's arrival?** Upstream has no oracle (its jobs leave no events). S17 decides both.
- **A second surface writer.** Only the compaction planner enforces "a replace never separates a call from its result", and the session invariant admits a `replace`-op `user/message` outside a step but not a `tool/result`. A pruner would be that second writer; the rule moves into the session when one exists.
- **Workspace-lifetime grants.** S15 shipped the session-lifetime shape and the identity any durable grant needs (tool plus exact subject), so what is left open is only the STORE. One that outlives a session cannot live in one log, `settings` may not own authority, and no workspace layer exists: a home-side store keyed by canonical root, COPIED into opening facts, is the only shape that keeps a stored log's audit self-contained.
- **A document input kind.** A PDF block is a vocabulary change (`AttachmentRef`, `ModelModality`, `ContentBlock`, both serializers, the meter); upstream's generic file kind, projected as one handle line and never bytes, gives every model PDF support for nothing.
- **What projection would a rename need?** `session/title` is last-wins, but nothing reads the CURRENT title cheaply: a name chosen at seq 5,000 is nowhere near the bounded prefix `persistence.list()` reads. A sidecar, a writer-maintained stamp, or a real projection store (upstream mounts one by default).
- **Two provider facts to design with.** DeepSeek's `user_id` gives each id its own rate budget, a seam for session identity; Anthropic caps a request at 32 MB with a 413 that maps to `INVALID_REQUEST`, which nothing retries and nothing compacts.
- **Does MiniDSH publish an importable surface?** A module-loaded plugin's own imports do not resolve from an installed package. An `exports` map is a compatibility promise; the first out-of-tree plugin decides, or S22 if its launcher hosts the runtime in-process rather than over the carrier. Skills do not need it.
- **How does a provider catalog stay current?** S11.5 found five Anthropic ids no prefix matched; on 2026-09-17 DeepSeek's `GET /models` listed `deepseek-flash` and `deepseek-v4-pro` while `deepseek-v4-flash` still answered. A snapshot in source, or fetched and cached: S18 meets it.

## 4. Development record

One entry per session: what it decided, and the measurements that still matter. The detail lives in the commits.

- **S16 Durable execution II: integrity and forensics (2026-09-26).** **What it decided:** the header's `version` is the ONLY format mark — BLUEPRINT's per-lifecycle format raised mid-log was dropped because `minidsh@1.0.0`, the only released reader, refuses by header alone and would continue a log a newer writer had marked; a line outside the closed envelope is damage, not format. Each lifecycle opens with `session/lifecycle`, whose `dispatch` and `durability: 'synced'` are explicit claims (the factory's, and what the store DECLARES), because presence-as-claim is the inference bug S14's whole-log "has ever dispatched" had — a downgrade hole. S14's "an event survived after the call" was unsound under a power cut (the gate writes `approval/*` BETWEEN the call and the dispatch), so a gate death reads not-started only in a synced lifecycle; salvage answers every owed call unknown, even one whose `tool/call` line was the damage. A flush resolves only after an `fdatasync` that STARTED after its last write — joining the sync in flight was a race the design critique found. **User decisions:** a weakened deployment acceptance is now stamped into the log (reversing S15's "a default, not a stamp"); replay stays in `test-support`. **Defects found:** `compose()` dropped `accepts`, so `serve/web/chat --accept` had done nothing since S15's fix; a resumed child fell back to the DEPLOYMENT acceptance for a narrowed mode; an unescalated `SANDBOX_UNAVAILABLE` reached the log as a ✓ with no code, which no audit could see. **The 7-lens review** (five diff lenses, the §4 collapse, a doc claim-diff) found what the tests did not: a cold fork of a log a LIVE writer holds told its model a gate-waiting call was safe to repeat (the store now reports the lease it reads, and such a fork is conservative); this session's own fix for the child fallback — its pin for every mode — re-opened the `read-only` widening `forMode` exists to stop; an escalation ran under an acceptance no line recorded; `verify` called intact logs that `inspect` and a resume then crashed on; a flush could resolve with no sync covering its write once its session detached, and a short sidecar write was followed by the truncate; and this build named itself `minidsh 1.0.0` in every composition record (now `1.1.0-dev`, its fixture re-recorded and both pinned by hash). **Measured:** `fdatasync` of one line over several runs, p50 0.7–3.6 ms / p95 0.9–4.4 on Windows NTFS, 2.4–10 / 4.7–20 ms on WSL 2 ext4 — the spread is host load (up to two per call, one per step, turn end and resume). **Live:** before the review, Windows 8/8 ×3; WSL/bwrap 6/8, 7/8, then 8/8 ×3 after two authority-arc premises were pinned (the model wrote with the shell; an unviewed file stopped at `FS_NOT_OBSERVED` before the fence) and a web-arc replay lost a file written by absolute path once; in all eight a cold `verify` between the kill and the resume predicted the closers the resume appended, byte for byte (the kill landed in the call window in 4). After the review fixes: Windows 7/8 (the web arc's premise: the model wrote `notes.txt` with the shell and paid a turn-1 escalation), that arc 3/3 once pinned, then 8/8; WSL/bwrap 8/8, a cold `verify` again predicting the resume's closers exactly. 883 tests on Windows (17 skipped), 899 under WSL 2 (1 skipped). **Fixtures:** the demo log no longer existed; two real logs were recorded (by `npx minidsh@1.0.0` and by this build) under a throwaway user once `ls -la` put an account name in a tool result, and the 1.0.0 replay found such a log cannot tell a gate error from a body error. A fixed 4-lens design critique before any code reshaped the plan more than any other step. 13 agents: 2 explore, 4 critics, 7 review.

- **S15.1 Documentation architecture reset (2026-09-24).** Docs only. **What it decided:** the documents are MAPS sized to a startup reading load, and a deletion test replaces "never delete a contract": S14's 0.37% floor came from that rule, not the material. **What it measured:** `ARCHITECTURE.md` 96,245 → 49,485 bytes (86% of a 56 KB ceiling: the map ~20k tokens of a ~40k startup read); `references/` 112,556 → 72,043; rule and reason sat at the owning code for 14 of 14 sampled contracts; the rewriters raised 15 suspected-false claims, one a false security claim (the bwrap hard link, ARCHITECTURE §13); a 5-lens review found 64 things (3 high, 11 medium), and the restorations landed ~2.5 KB over the planned 47 KB. **Live:** the same 8 placement questions through `minidsh run` (DeepSeek) against each map alone: old 8/8, new 8/8, the new read at half the tokens (83k vs 166k). 17 agents.

- **S15 Authority II: consent on the record (2026-09-23).** **What it decided** (the reasons its code does not state): the subject rides the EVENT and the joined call the VIEW, because prompting off `session.view` hangs the terminal — `session.event` is backlogged while unattached and `session.view` discarded, and an agent parked inside `request` publishes no later view. A four-lens design critique before any code changed five things and cut three speculative vocabularies with no reader: `NETWORK_TOOL`, `PreToolDecision.ask.subject`, `--accept partial`. **Found:** DSH research falsified three `references/` claims, corrected there (`assumptions.md`, `dsh/authority.md`); only a live arc found a real escape (a delegated child accepting `none` for itself); only the 5-lens review found the session's headline defect, a many-to-one subject rendering sharing one grant key, reproduced in the parent before it was believed; the same review found `--accept` wired into none of the four commands that listed it (S16 found `compose()` still dropped it). **Measured live:** `second turn: 1 decision(s), 1 answered by the grant`; `accepting session ran 1 shell command(s) with 0 approvals`, the Windows fix against a real model. 809 tests on Windows, 824 under WSL 2; 15 agents against a ceiling of 16 stated in the plan. `ARCHITECTURE.md` ended at 100% of its 94 KB ceiling.

- **S14 Durable execution I (2026-09-20).** **What it decided:** the DRIVER writes the gate-to-body fact, the compaction closer says `unclosed`, effects fold per step (each reason is at its code); DSH research changed nothing structural. **The 5-lens review found a regression the session had shipped:** repair read a missing dispatch as proof in an unsynced log; its patch is the rule S16 found unsound. **Nine tests for what nothing covered:** no `tools/execute` listener existed anywhere, so hoisting the call out of the waterfall passed all 736. **What it measured:** three compressors told never to delete a claim freed 0.37% of `ARCHITECTURE.md`, so the ceiling moved to 94 KB. 752 tests; 4 research + 3 compaction + 5 review agents, and a plan-mode design critique worth more than any of them.

- **S13 Route and references (2026-09-19 → 09-20).** **Decided:** a job fact lies outside the step-scoped set and needs no invariant relaxation, but a `tool/result` pairs within ONE step, so an asynchronous completion re-enters through the inbox. One fixed 14-agent workflow (six assessors, eight DSH researchers pinned to `ddefc45f`; 1.83M subagent tokens). 712 tests. **The harness silently RESUMES a workflow a usage limit killed**, at the reset: read its journal and transcript mtimes before relaunching anything.

- **Docs compaction (2026-09-10 → 09-11).** The documents moved under `docs/`: `ARCHITECTURE.md` 95 → 74 KB, `BLUEPRINT.md` 27 → 21 KB, `README.md` 48 → 28 KB. **Measured:** a pass told never to delete a contract stopped at 90% of the original, as three earlier rewrites had; binding per-section maxima plus a worked example reached 52–65%. Its review enumerated 1,334 normative claims and raised 235 findings, 124 above narrative, plus one invented claim; restoring them cost ~7 KB. 25 subagents, ~3.5M tokens.

- **V1 release (2026-09-09).** 19,777 lines of TypeScript in 110 files, 1,277 of browser client, 18,027 of tests in 71. Gated on the exact bytes that ship: the packed tarball (119 entries, 251 KB) fixed a failing fixture through its installed bin. **Arcs three times per host:** Windows 8/8, 7/8, 8/8; Linux under bwrap 8/8, 8/8, 7/8; `live.yml` ubuntu 8/8, macOS 6/8 then 2/2 filtered — every failure the routing arc's marker drop with `history_read` called 0× (7 of 10). **Publication facts a release meets again:** npm's 2FA challenge needs an interactive terminal, so a person types `npm publish`; npm logs a rewritten `bin` entry as "invalid and removed", so `package.json` spells it `bin/x`; GitHub reads the license as "Other" unless LICENSE is pure MIT with the attribution in NOTICE. DSH links rot within weeks (five dead in one check). ~1.5 CNY, ~590k subagent tokens.

- **S11.5 Hardening checkpoint / release candidate (2026-09-07 → 09-08).** **CI found what no local run could:** two assertions comparing a workspace root to itself (`/private/var` vs `/var`; an 8.3 short TEMP), an `EPIPE` on macOS alone, and a `MINIDSH_EXPECT_SHELL` gate that had never executed. 676 tests; `live.yml` green under bwrap and Seatbelt. ~3 CNY.

- **S11 Confinement & CI (2026-09-06).** The hint on the kernel's own denial, without which a confined host is an authority regression, costs +48 tokens of fixed prefix per request. 668 tests; ~2.1 CNY.

- **S10 Session & product surfaces (2026-09-06).** A browser turn went from 197 node insertions and 25 clears to 16 and none once a delta stopped rebuilding the transcript. 636 tests.

- **S9 Context & prompt hygiene (2026-09-05).** Bounded recall. A review fanned out per finding reached 46 agents before the user killed it, the origin of CLAUDE.md §8's fixed count. 601 tests.

- **S8.5 Architecture review (2026-09-04 → 09-05).** One bounded workflow against the current DSH (192 claims, 228 fetches) overturned three plans: extension authoring, session deletion, and where the sandbox-mode prompt sentence lives. 581 tests.

- **S1–S8 Foundation through verification (2026-08-22 → 09-02).** The kernel, the core spine, the DeepSeek adapter, JSONL persistence and the headless CLI (S1); two surfaces over one runtime and the durable op-shaped inbox (S2); authority as one replayable plane, the fence in the fs PROVIDER (S3: untested enforcement is worse than an honest `none`); what MiniDSH *is* became data (S4); metering, the compaction seam, spill, workspace instructions (S5); the model as a capability the runtime routes to (S6); the web surface and tier-aware backpressure (S7: a 400-turn session is 103,201 events and 17.0 MB); the attachment plane and a verifier that is a composition row (S8). The checkpoints S1.5, S5.5 and S7.5 found three documented invariants untrue in code, a crash repair that resumed a history every OpenAI-compatible wire refuses, and a wire client that could prompt a LIVE delegated child.
